use toolkit::input::get_input;

fn main() {
    let contents = get_input();
    println!("{}", contents);
}
#[cfg(test)]
mod tests {
    #[test]
    fn test_0_cookie_cutter() {
        assert_eq!(1, 1)
    }
}
